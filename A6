

print("A6--11  ")


local scr = game:GetService("ReplicatedStorage").Collectors
local scv = require(scr)

for key, value in pairs(scv) do
    print("Anahtar:", key, "| Değer Tipi:", type(value))
    
    -- Eğer içeriği bir tablo ise alt elemanlarını da yazdıralım
    if type(value) == "table" then
        for subKey, subValue in pairs(value) do
            print("   ->", subKey, ":", tostring(subValue))
        end
    end
end

print("SON")



