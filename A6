
--A6 1

for i,v in pairs(require(game:GetService("ReplicatedStorage").Collectors.LocalCollect)) do if tostring(i)=="GetCollectorCooldown" then
print("Function Bulundu")
hookfunction(v,function() 

	
	local v2 = require(game.ReplicatedStorage.Collectors).GetStat(v1.EquippedCollector, "Cooldown")
	print(v2)
	v2 = v2 / 2
	return v2

end) end end
print("islem uygulandi")



